namespace SupremEPRSystemForRealBussiness.src;
using TECHCOOL.UI;

class SupremEPRSystem
{
    static void Main (string[] args)
    {
        Menu menu = new Menu();
        Screen.Display(menu);
    }
}
